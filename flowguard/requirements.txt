# FlowGuard — Python dependencies
# Install: pip install -r requirements.txt
#
# After install, optionally download spaCy model:
#   python -m spacy download en_core_web_sm

# ── Web framework ──────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9      # multipart file uploads

# ── Data validation ────────────────────────────────────────────────────
pydantic>=2.7.0

# ── Database ORM ───────────────────────────────────────────────────────
sqlalchemy>=2.0

# ── LLM Layer: Groq API ───────────────────────────────────────────────
groq>=0.9.0
python-dotenv>=1.0.0

# ── File Import: PDF ──────────────────────────────────────────────────
pdfplumber>=0.10              # PDF text + table extraction
pypdfium2>=4.0                # PDF page rendering for scanned PDFs
Pillow>=10.0.0                # Image processing + PNG→JPEG conversion

# ── File Import: Excel / CSV ──────────────────────────────────────────
pandas>=2.0
openpyxl>=3.1.0               # .xlsx read/write
xlrd>=2.0.1                   # .xls legacy support

# ── HTTP client (for tests) ───────────────────────────────────────────
requests>=2.31.0

# ── NLP (optional — graceful fallback to regex if not installed) ──────
# spacy>=3.7.0
# sentence-transformers>=2.7.0
# torch>=2.0.0

# ── PDF table extraction (optional — needs Ghostscript binary) ─────────
# camelot-py[cv]>=0.11

# ── Testing ────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# ── WhatsApp webhook (optional) ────────────────────────────────────────
# twilio>=9.0.0
